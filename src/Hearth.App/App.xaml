<Application x:Class="Hearth.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <ResourceDictionary>

      <!-- Label colours. Icons sit on arbitrary wallpaper, so text needs its
           own contrast rather than inheriting a theme background. -->
      <Color x:Key="LabelColor">#FFFFFFFF</Color>
      <Color x:Key="LabelShadowColor">#CC000000</Color>
      <SolidColorBrush x:Key="LabelBrush" Color="{StaticResource LabelColor}" />

      <!-- Selection and hover affordances, tuned to read over both a bright
           photo and a dark solid colour. -->
      <SolidColorBrush x:Key="TileHoverBrush" Color="#26FFFFFF" />
      <SolidColorBrush x:Key="TileSelectedBrush" Color="#40FFFFFF" />
      <SolidColorBrush x:Key="DropTargetBrush" Color="#5900A8FF" />

      <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#F21C1C1F" />
      <SolidColorBrush x:Key="MenuForegroundBrush" Color="#FFF2F2F4" />
      <SolidColorBrush x:Key="MenuBorderBrush" Color="#26FFFFFF" />
      <SolidColorBrush x:Key="MenuHighlightBrush" Color="#33FFFFFF" />
      <SolidColorBrush x:Key="MenuSeparatorBrush" Color="#1FFFFFFF" />

      <!-- Icon labels get a soft shadow instead of a plate behind them: it
           survives a white wallpaper without boxing every tile in. -->
      <Style x:Key="IconLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource LabelBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="MaxHeight" Value="34" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="Effect">
          <Setter.Value>
            <DropShadowEffect BlurRadius="4" ShadowDepth="1" Direction="270"
                              Color="{StaticResource LabelShadowColor}" Opacity="0.9" />
          </Setter.Value>
        </Setter>
      </Style>

      <!--
        MenuItem needs a full ControlTemplate, not just colour setters.

        A Background setter styles the item ROW only. The submenu flyout is a
        separate Popup built by the default template, and it paints itself with
        the system menu brush — so a dark theme with light text produces white
        text on a white submenu. The only way to colour that popup is to own the
        template that creates it.
      -->
      <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="MenuItem">
              <Grid>
                <Border x:Name="Row"
                        Background="{TemplateBinding Background}"
                        CornerRadius="4" Margin="2,1">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="24" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Icon, for jump list entries. -->
                    <ContentPresenter x:Name="IconHost" Grid.Column="0"
                                      ContentSource="Icon"
                                      Width="16" Height="16"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" />

                    <!-- Check mark for the toggleable entries. -->
                    <Path x:Name="Check" Grid.Column="0"
                          Visibility="Collapsed"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Width="12" Height="12" Stretch="Uniform"
                          Fill="{StaticResource MenuForegroundBrush}"
                          Data="M 0,5 L 4,9 L 11,1 L 9.5,0 L 4,6.5 L 1.2,4 Z" />

                    <ContentPresenter Grid.Column="1"
                                      ContentSource="Header"
                                      RecognizesAccessKey="True"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center" />

                    <!-- Submenu arrow. -->
                    <Path x:Name="Arrow" Grid.Column="2"
                          Margin="12,0,8,0"
                          VerticalAlignment="Center"
                          Width="5" Height="9" Stretch="Uniform"
                          Fill="{StaticResource MenuForegroundBrush}"
                          Data="M 0,0 L 5,4.5 L 0,9 Z" />
                  </Grid>
                </Border>

                <!-- Must be named PART_Popup: MenuItem drives it by name. -->
                <Popup x:Name="PART_Popup"
                       Placement="Right"
                       HorizontalOffset="-6"
                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Fade">
                  <Border Background="{StaticResource MenuBackgroundBrush}"
                          BorderBrush="{StaticResource MenuBorderBrush}"
                          BorderThickness="1"
                          CornerRadius="6"
                          Padding="4"
                          SnapsToDevicePixels="True">
                    <StackPanel IsItemsHost="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle" />
                  </Border>
                </Popup>
              </Grid>

              <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                  <Setter TargetName="Row" Property="Background" Value="{StaticResource MenuHighlightBrush}" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                  <Setter TargetName="IconHost" Property="Visibility" Value="Collapsed" />
                </Trigger>
                <Trigger Property="HasItems" Value="False">
                  <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" Value="0.4" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style TargetType="ContextMenu">
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ContextMenu">
              <Border Background="{StaticResource MenuBackgroundBrush}"
                      BorderBrush="{StaticResource MenuBorderBrush}"
                      BorderThickness="1"
                      CornerRadius="6"
                      Padding="4"
                      SnapsToDevicePixels="True">
                <StackPanel IsItemsHost="True"
                            KeyboardNavigation.DirectionalNavigation="Cycle" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style TargetType="Separator">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Separator">
              <Rectangle Height="1" Margin="8,4"
                         Fill="{StaticResource MenuSeparatorBrush}" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

    </ResourceDictionary>
  </Application.Resources>
</Application>
